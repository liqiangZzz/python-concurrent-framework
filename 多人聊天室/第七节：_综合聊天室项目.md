# 第七节： 综合聊天室项目

讲师：肖斌

# 一、聊天室项目需求

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=ZjBjMzE5MTJmZjE3NzE1MWVlYTg5OWUyN2Q5MmI0ODNfN2RkYWVkMjNkZTk3NTk5YmQ3ZmRhNWI3MGRkZTVjZGNfSUQ6NzI5NDg1MjI4NTgwNzU3NTA0MV8xNzc5OTc2NzY1OjE3ODAwNjMxNjVfVjM)



- 服务器只有一个，打开界面后点击“启动”，开启socket服务

- 每个人都可以打开一个客户端界面，每个人进来之前都必须定义一个名字

- 客户端点击“连接”，进入聊天室，进入之后，服务器发出通知，每个人都能看到

- 客户端可以发送一条消息到聊天室，所有人都可以看到刚刚发送的该条消息

- 客户端可以点击“离开”退出聊天室。服务器也要发出通知

- 服务器点击“聊天记录保存”，保存聊天信息到文件中

- 服务器点击“停止”，所有人都退出聊天室（发通知），并且服务器窗口关闭。（作业）

    - 1、注意recv函数是阻塞

    - 2、先要发送通知给所有的客户端用户，之后再给所有客户端用户发送一个规定的内容（判断服务关闭确认）

    - 3、服务器的窗口要关闭，客户端用户的窗口不关闭。 

# 二、界面开发

## 1、wxPython入门

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=NTQ5Y2UxM2FlYmZlZmE4N2MzM2MyZTYxYWFjMTVlYmVfY2JiM2Q2YWY2NDhlNTE3YjlhNjNmZTExZjU1ZTNhOTlfSUQ6NzI5NTU4NTY5NTczNjg0MDE5Nl8xNzc5OTc2NzY1OjE3ODAwNjMxNjVfVjM)

### （1） wxPython介绍

Python 的 GUI 开发框架有好几种，比较主流的有 wxPython、PyQt5、Tkinter，这三种都是跨平台方案。

wxPython 是 Python 的第三方库，代码实现基于 C\+\+ 的 wxWidgets 库封装，呈现的界面风格和系统本地风格一致。其许可协议规定如果直接引用 wxPython 的二进制库文件，则可以随便使用。

```Python
import wx
app = wx.App()
frame = wx.Frame(None, -1, "Hello, World!")
frame.Show(True)
app.MainLoop()

```

### （2）创建应用实例：

```Plain Text
app = wx.App()
```

每一个wxPython程序都必须有一个应用实例。

### （3）创建应用框架并显示：

```Plain Text
frame = wx.Frame(None, title='Hello World')
frame.Show()
```

这里创建了一个wx\.Frame对象。wx\.Frame是一个重要的“容器”组件，它用来承载其他组件，它本身没有父组件（如果我们给组件的parent参数设为None，即代表该组件没有父组件）。创建该对象后，还需调用Show方法才能显示出来。

wx\.Frame的构造函数一共有七个参数：

```Plain Text
wx.Frame(wx.Window parent, int id=-1, string title='', wx.Point pos=wx.DefaultPosition,
    wx.Size size=wx.DefaultSize, style=wx.DEFAULT_FRAME_STYLE, string name="frame")
```

除了第一个parent参数需要显式指定，其余六个都有默认值，包括ID、名称、位置、尺寸和样式等。因此，可以通过改变这些参数来进一步地对该frame进行个性化定制。

### （4）启动程序主循环：

```Plain Text
app.MainLoop()
```

程序的主循环是一个无限循环模式，它捕获并分发程序生命周期内的所有事件。

## 2、布局管理

布局可以分为绝对布局和布局管理器sizer。绝对布局有很多缺点，比如：

（1）组件的尺寸和位置不随窗口的改变而改变；

（2）不同平台上应用程序可能显示不同；

（3）字体的改变可能破坏布局；

（4）如果想改变布局，必须将之前的全部推翻。

因此，推荐使用布局管理器sizer来管理布局。wxPython常用的sizer有：wx\.BoxSizer、wx\.StaticBoxSizer、wx\.GridSizer、wx\.FlexGridSizer、wx\.GridBagSizer。

### （1）wx\.BoxSizer

wx\.BoxSizer是最常见的布局管理器。它的常用设置有：

（1）排列方向：wx\.VERTICAL垂直排列还是wx\.HORIZONTAL水平排列；

（2）对齐方式：可以设定左端对齐、右端对齐、顶部对齐、底部对齐、中心对齐等多种对齐方式；

（3）在某一级容器组件中，使用SetSizer\(\)来为其指定布局管理器；

（4）在布局管理器中用Add\(\)方法来添加组件。

### （2）wx\.GridSizer

wx\.GridSizer是网格布局管理器，可以设置几排几列以及横纵的间距，网格中的组件尺寸都是相同的。

### （3）wx\.FlexGridSizer

wx\.FlexGridSizer与wx\.GridSizer类似，但其更灵活，它不要求网格中所有的组件尺寸都相同，而是在同一行中的所有组件都高度相同，而同一列中的所有组件都宽度相同。



